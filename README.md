## Installation
download or clone repository and run
    ```
    npm install
    ```
to run 
    ```
    npm run dev
    ```
to test all run
    ```
    npm test
    ```

>the test contains the examples given in the pdf
